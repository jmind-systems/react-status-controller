import { Statuses } from 'types';

export const defaultReduxStatuses: Statuses = {
  none: 'none',
  loading: 'loading',
  error: 'error',
  success: 'success'
}

export const colors = {
  white: '#fff',
  blue: '#1A3D98'
}
